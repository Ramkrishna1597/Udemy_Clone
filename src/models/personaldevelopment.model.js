const mongoose = require("mongoose")

const personaldevelopmentSchema = new mongoose.Schema(
    
)
const PersonalDevelopment = mongoose.model("personaldevelopment",personaldevelopmentSchema);

module.exports=PersonalDevelopment  